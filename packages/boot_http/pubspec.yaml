name: boot_http
description: HTTP server for Boot (@Controller, routing, filters).
repository: https://github.com/zonnedev/dart-boot/tree/master/packages/boot_http
version: 0.1.2+2
environment:
  sdk: ^3.5.0
resolution: workspace
dependencies:
  boot_core: ^0.1.3
  boot_aop: ^0.1.1+4
  boot_http_common: ^0.1.2+1
  boot_security: ^0.1.1+2
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  path: ^1.9.0

dev_dependencies:
  test: ^1.25.0

topics:
  - boot
  - server
  - http
  - routing
  - websocket
