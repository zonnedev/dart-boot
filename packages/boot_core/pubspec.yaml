name: boot_core
description: Core DI annotations, container, and configuration for the Boot framework.
repository: https://github.com/zonnedev/dart-boot/tree/master/packages/boot_core
version: 0.1.3
environment:
  sdk: ^3.5.0
resolution: workspace
dependencies:
  yaml: ^3.1.0

dev_dependencies:
  test: ^1.25.0

topics:
  - boot
  - dependency-injection
  - configuration
  - logging
