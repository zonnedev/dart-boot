name: boot_http_client
description: HTTP client for Boot (@Client, HttpClientBuilder).
repository: https://github.com/zonnedev/dart-boot/tree/master/packages/boot_http_client
version: 0.1.3+1
environment:
  sdk: ^3.5.0
resolution: workspace
dependencies:
  boot_core: ^0.2.0+1
  boot_aop: ^0.2.0+1
  boot_http_common: ^0.2.0+1
  boot_http: ^0.2.0+1
  http: ^1.0.0

dev_dependencies:
  boot_generator:
    path: ../boot_generator
  build_runner: ^2.4.0
  test: ^1.25.0

topics:
  - boot
  - http-client
  - http
