name: boot_aop_generator
description: Code generator for Boot AOP proxies (@Around interceptors).
repository: https://github.com/zonnedev/dart-boot/tree/master/packages/boot_aop_generator
version: 0.1.0

environment:
  sdk: ^3.5.0

resolution: workspace

dependencies:
  boot_core: ^0.1.3
  boot_aop: ^0.1.1+4
  analyzer: ^7.0.0
  build: ^2.4.0
  source_gen: ^2.0.0

topics:
  - boot
  - codegen
  - aop
  - build-runner
