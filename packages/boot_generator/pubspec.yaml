name: boot_generator
description: Code generators for the Boot framework.
repository: https://github.com/zonnedev/dart-boot/tree/main/packages/boot_generator
version: 0.1.1+2

environment:
  sdk: ^3.5.0

resolution: workspace

dependencies:
  boot_core: ^0.1.0
  boot_aop: ^0.1.0
  boot_events: ^0.1.0
  boot_scheduling: ^0.1.0
  boot_http: ^0.1.0
  boot_http_common: ^0.1.0
  boot_http_client: ^0.1.0
  boot_serialization: ^0.1.0
  build: ^2.4.0
  source_gen: ^2.0.0
  analyzer: ^7.0.0
  glob: ^2.1.0
  yaml: ^3.1.0

dev_dependencies:
  build_runner: ^2.4.0
  build_test: ^2.2.0
  source_gen_test: ^1.0.0
  test: ^1.25.0

topics:
  - boot
  - codegen
  - build-runner
  - dependency-injection
