name: boot_events
description: Event system for Boot (EventBus, @EventListener).
repository: https://github.com/zonnedev/dart-boot/tree/master/packages/boot_events
version: 0.1.1+4
environment:
  sdk: ^3.5.0
resolution: workspace
dependencies:
  boot_core: ^0.1.3

dev_dependencies:
  test: ^1.25.0

topics:
  - boot
  - events
  - event-bus
