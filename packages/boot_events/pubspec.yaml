name: boot_events
description: Event system for Boot (EventBus, @EventListener).
repository: https://github.com/zonnedev/dart-boot/tree/main/packages/boot_events
version: 0.1.1
environment:
  sdk: ^3.5.0
resolution: workspace
dependencies:
  boot_core:
    path: ../boot_core

dev_dependencies:
  test: ^1.25.0

topics:
  - boot
  - events
  - event-bus
