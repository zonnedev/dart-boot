services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      BOOT_ENV: prod
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health/"]
      interval: 10s
      timeout: 5s
      retries: 3
