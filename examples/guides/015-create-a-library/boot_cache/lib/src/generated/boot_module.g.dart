// GENERATED by boot_generator — do not edit.
// ignore_for_file: no_leading_underscores_for_library_prefixes
import 'package:boot/boot.dart';
import 'package:boot_cache/src/mongo_token_store.dart';
import 'package:boot_token/src/token_store.dart';
import 'package:boot_token/src/generated/boot_module.g.dart';

/// Module function for the boot_cache library.
/// Called by the consuming app's $configure() to wire this library's beans.
void $BootCacheModule(BeanContainer container, BootRouter router, BootConfig config, List<void Function()> deferred) {
  if (container.hasModule('boot_cache')) return;
  container.markModule('boot_cache');

  $BootTokenModule(container, router, config, deferred);



  deferred.add(() { if ((config.get('mongo.enabled')) == 'true' && !container.has<TokenStore>()) {
      container.register<MongoTokenStore>($MongoTokenStoreDefinition());
  container.register<TokenStore>($MongoTokenStoreDefinition());
    } });


















}
