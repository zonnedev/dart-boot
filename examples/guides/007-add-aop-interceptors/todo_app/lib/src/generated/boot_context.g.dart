// GENERATED by boot_generator — do not edit.
// ignore_for_file: no_leading_underscores_for_library_prefixes
import 'package:boot/boot.dart';
import 'package:todo_app/src/controllers/hello_controller.dart';
import 'package:todo_app/src/services/product_service.dart';
import 'package:todo_app/src/controllers/product_controller.dart';
import 'package:todo_app/src/aop/timed_interceptor.dart';
import 'package:boot_core/src/container/method_interceptor.dart';
import 'package:todo_app/src/aop/cached_interceptor.dart';
import 'package:todo_app/src/aop/timed.dart';
import 'package:todo_app/src/aop/cached.dart';


class _ContainerSelfDefinition extends BeanDefinition {
  final BeanContainer _container;
  _ContainerSelfDefinition(this._container);
  @override
  Type get beanType => BeanContainer;
  @override
  dynamic create(BeanContainer container) => _container;
}

void $configure(BeanContainer container, BootRouter router) {
  // Self-register the container
  container.register<BeanContainer>(_ContainerSelfDefinition(container));

  final config = container.get<BootConfig>();
  final deferred = <void Function()>[];

  // Load library modules


  // Register beans in dependency order
  container.register<HelloController>($HelloControllerDefinition());
  container.register<ProductService>($ProductService$ProxyDefinition());
  container.register<ProductController>($ProductControllerDefinition());
  container.register<TimedInterceptor>($TimedInterceptorDefinition());
  container.register<MethodInterceptor>($TimedInterceptorDefinition());
  container.register<CachedInterceptor>($CachedInterceptorDefinition());
  container.register<MethodInterceptor>($CachedInterceptorDefinition());

  // Evaluate deferred beans (beans/missingBeans conditions)

  for (final d in deferred.reversed) { d(); }

  // Register interceptors
  container.registerInterceptor(Timed, container.get<TimedInterceptor>());
  container.registerInterceptor(Cached, container.get<CachedInterceptor>());

  // Register routes
  router.addAll($HelloControllerRoutes(container.get<HelloController>()).routes);
  router.addAll($ProductControllerRoutes(container.get<ProductController>()).routes);
}
