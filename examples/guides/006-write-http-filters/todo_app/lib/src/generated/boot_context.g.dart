// GENERATED by boot_generator — do not edit.
// ignore_for_file: no_leading_underscores_for_library_prefixes
import 'package:boot/boot.dart';
import 'package:todo_app/src/controllers/hello_controller.dart';
import 'package:todo_app/src/controllers/todo_controller.dart';
import 'package:todo_app/src/filters/timing_filter.dart';
import 'package:boot_http_common/src/http/filter.dart';
import 'package:todo_app/src/filters/request_id_filter.dart';
import 'package:todo_app/src/filters/rate_limit_filter.dart';


class _ContainerSelfDefinition extends BeanDefinition {
  final BeanContainer _container;
  _ContainerSelfDefinition(this._container);
  @override
  String get typeName => 'BeanContainer';
  @override
  dynamic create(BeanContainer container) => _container;
}

void $configure(BeanContainer container, BootRouter router) {
  // Self-register the container
  container.register<BeanContainer>(_ContainerSelfDefinition(container));

  final config = container.get<BootConfig>();
  final deferred = <void Function()>[];

  // Load library modules


  // Register beans in dependency order
  container.register<HelloController>($HelloControllerDefinition());
  container.register<TodoController>($TodoControllerDefinition());
  container.register<TimingFilter>($TimingFilterDefinition());
  container.register<HttpServerFilter>($TimingFilterDefinition());
  container.register<RequestIdFilter>($RequestIdFilterDefinition());
  container.register<HttpServerFilter>($RequestIdFilterDefinition());
  container.register<RateLimitFilter>($RateLimitFilterDefinition());
  container.register<HttpServerFilter>($RateLimitFilterDefinition());

  // Evaluate deferred beans (beans/missingBeans conditions)

  for (final d in deferred.reversed) { d(); }

  // Register interceptors


  // Register client filters


  // Register server filters
  router.addFilter('/**', container.get<TimingFilter>(), order: 2);
  router.addFilter('/**', container.get<RequestIdFilter>(), order: 1);
  router.addFilter('/**', container.get<RateLimitFilter>(), order: 0);

  // Register exception handlers


  // Register authentication providers


  // Register health indicators


  // Register WebSocket handlers


  // Register routes
  router.addAll($HelloControllerRoutes(container.get<HelloController>()).routes);
  router.addAll($TodoControllerRoutes(container.get<TodoController>()).routes);

  // Register event listeners


  // Register scheduled tasks

}
