// GENERATED by boot_generator — do not edit.
// ignore_for_file: no_leading_underscores_for_library_prefixes
import 'package:boot/boot.dart';
import 'package:todo_app/src/services/notification_dispatcher.dart';
import 'package:todo_app/src/controllers/notification_controller.dart';
import 'package:todo_app/src/controllers/hello_controller.dart';
import 'package:todo_app/src/services/sms_channel.dart';
import 'package:todo_app/src/services/notification_channel.dart';
import 'package:todo_app/src/services/push_channel.dart';
import 'package:todo_app/src/services/email_channel.dart';


class _ContainerSelfDefinition extends BeanDefinition {
  final BeanContainer _container;
  _ContainerSelfDefinition(this._container);
  @override
  Type get beanType => BeanContainer;
  @override
  dynamic create(BeanContainer container) => _container;
}

void $configure(BeanContainer container, BootRouter router) {
  // Self-register the container
  container.register<BeanContainer>(_ContainerSelfDefinition(container));

  final config = container.get<BootConfig>();
  final deferred = <void Function()>[];

  // Load library modules


  // Register beans in dependency order
  container.register<NotificationDispatcher>($NotificationDispatcherDefinition());
  container.register<NotificationController>($NotificationControllerDefinition());
  container.register<HelloController>($HelloControllerDefinition());
  container.register<SmsChannel>($SmsChannelDefinition());
  container.registerNamed<NotificationChannel>('sms', $SmsChannelDefinition());
  container.register<PushChannel>($PushChannelDefinition());
  container.registerNamed<NotificationChannel>('push', $PushChannelDefinition());
  container.register<EmailChannel>($EmailChannelDefinition());
  container.registerPrimary<NotificationChannel>($EmailChannelDefinition());

  // Evaluate deferred beans (beans/missingBeans conditions)

  for (final d in deferred.reversed) { d(); }

  // Register interceptors


  // Register routes
  router.addAll($NotificationControllerRoutes(container.get<NotificationController>()).routes);
  router.addAll($HelloControllerRoutes(container.get<HelloController>()).routes);
}
